version: '3.7'

services:
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile_prod
    container_name: frontend_prod
    ports:
      - 8080:8080
    volumes:
      - dist:/app/dist
    expose:
      - 8080


